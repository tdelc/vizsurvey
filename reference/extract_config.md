# extract a config from key (config from load_config)

extract a config from key (config from load_config)

## Usage

``` r
extract_config(config, key_)
```

## Arguments

- config:

  df of configuration

- key\_:

  key to extract

## Value

string
