# Vecteur de groupe "VAR = valeur" / "VAR != valeur" pour les comparaisons

Vecteur de groupe "VAR = valeur" / "VAR != valeur" pour les comparaisons

## Usage

``` r
group_vs_others(x, value, label)
```

## Arguments

- x:

  Vecteur a regrouper

- value:

  valeur discrétionnaire

- label:

  label à utiliser

## Value

vector
