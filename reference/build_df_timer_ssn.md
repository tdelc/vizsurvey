# Build the df_timer of sessions

Build the df_timer of sessions

## Usage

``` r
build_df_timer_ssn(df_timer, df_timer_sctn, cfg)
```

## Arguments

- df_timer:

  df_timer

- df_timer_sctn:

  df_timer_sctn

- cfg:

  config list

## Value

data.frame
